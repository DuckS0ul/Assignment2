#include "../../include/components/VelocityComponent.h"
#include "../../include/entities/Entity.h"
#include <iostream>

//void VelocityComponent::update(Entity& e, float elapsed) {
//	float x = e.getPosition().x + velocityDirection.x * speed * elapsed;
//	float y = e.getPosition().y + velocityDirection.y * speed * elapsed;
//	e.setPosition(x, y);
//}

//ComponentID VelocityComponent::getID() const {
//    return ComponentID::VELOCITY;
//}